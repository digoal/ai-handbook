<!--
 Licensed to the Apache Software Foundation (ASF) under one
 or more contributor license agreements.  See the NOTICE file
 distributed with this work for additional information
 regarding copyright ownership.  The ASF licenses this file
 to you under the Apache License, Version 2.0 (the
 "License"); you may not use this file except in compliance
 with the License.  You may obtain a copy of the License at

   http://www.apache.org/licenses/LICENSE-2.0

 Unless required by applicable law or agreed to in writing,
 software distributed under the License is distributed on an
 "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 KIND, either express or implied.  See the License for the
 specific language governing permissions and limitations
 under the License.
-->

# 附录 · 词汇表与速查表

> **Abstract** — This appendix is the handbook's reference section: A. Glossary (50+ terms with bilingual labels), B. Field reference (top-level down to `custom_extensions`), C. Converter quick lookup (CLI + install), D. Error message reference, E. CLI command reference, F. FAQ (7 common questions), G. Reference links, H. Source file index (each chapter → source files). v1.1 additions: I. Concept index (term → chapter anchor), J. Handbook maintenance guide.

> 本附录是手册的"参考手册"——遇到具体字段、错误、命令时翻这里。

## A. 词汇表（中英对照）

> v1.1 扩展：18 → 56 术语。涵盖核心概念、SDK、CLI、治理、生态。

### A.1 核心概念

| 中文 | 英文 | 含义 |
|---|---|---|
| 语义模型 | Semantic Model | 业务语义层描述，包含数据集、关系、度量 |
| 语义模型文档 | `OSIDocument` | 顶层 YAML/JSON 文档，含 `version` + `semantic_model[]` |
| 数据集 | Dataset | 逻辑表（fact/dimension），对应物理表或子查询 |
| 字段 | Field | 行级属性，可分组、过滤、作为度量表达式操作数 |
| 关系 | Relationship | 外键连接，声明多对一 |
| 度量 | Metric | 跨数据集聚合表达式 |
| 维度 | Dimension | `Field` 的角色标记（当前仅含 `is_time`） |
| 时间维度 | Time Dimension | `dimension.is_time: true` 标记的字段 |
| 度量上下文 | `from_dataset` | Python 端 `Relationship.from` 的别名（避关键字） |
| 时间维度判定 | `is_time_dimension()` | `OSIField` 方法，实现显式 > 默认规则 |
| 方言 | Dialect | SQL 方言变体（7 种） |
| 数据类型 | DataType | 10 种可移植逻辑类型 |
| 时间角色 | is_time | 字段作为时间维度的标记 |
| 表达式 | `Expression` | 含 `dialects[]` 数组的多方言 SQL 容器 |
| 方言表达式 | `DialectExpression` | 单个 `(dialect, expression)` 对 |
| 自定义扩展 | Custom Extension | 厂商特定的 metadata |
| AI 上下文 | AI Context | LLM agent 用的业务注解 |
| AI 上下文对象 | `AIContextObject` | 含 `instructions` / `synonyms` / `examples` 的结构化形态 |
| 往返保真 | Round-trip fidelity | 自定义扩展在多轮转换后仍保留 |
| 维度 vs 角色 | Dimension vs Role | `datatype` 是类型，`is_time` 是角色，二者独立 |

### A.2 本体层

| 中文 | 英文 | 含义 |
|---|---|---|
| 本体 | Ontology | 业务概念层（可选超集） |
| 实体类型 | `EntityType` | 真实世界对象 |
| 值类型 | `ValueType` | 带约束的 scalar 类型 |
| 内置类型 | Built-in Types | `Any` / `Boolean` / `Date` / `DateTime` / `Decimal` / `Float` / `Integer` / `String` |
| 多重性 | `Multiplicity` | 关系 cardinality（`ManyToOne` / `OneToOne`） |
| 派生概念 | `derived_by` | 基于其他数据派生 entity 人口的表达式 |
| 派生关系 | `Derived Relationship` | 通过 `derived_by` 表达式计算的 relationship |
| 概念映射 | `OntologyMap` | 逻辑模型 → ontology 概念的桥 |
| 链映射 | `LinkMapping` | relationship 中每个 link 的映射 |
| 概念互操作 | Conceptual Interoperability | 跨模型业务概念对齐 |
| 结构互操作 | Structural Interoperability | 跨工具格式互通 |
| 标识符映射 | `ReferentMapping` | 逻辑字段绑定到 identifying relationship |

### A.3 验证与 SDK

| 中文 | 英文 | 含义 |
|---|---|---|
| 验证器 | Validator | `validation/validate.py` |
| Pydantic v2 | — | `apache-ossie` 用的类型系统 |
| `frozen=True` | — | 模型实例不可变 |
| `frozen=True, extra="allow"` | — | 向前兼容新字段 |
| `by_alias=True` | — | 序列化时用 `from` 而非 `from_dataset` |
| `exclude_none=True` | — | 序列化时省略 None 字段 |
| `mode="json"` | — | 序列化严格 JSON 兼容 |
| `populate_by_name=True` | — | 同时接受 `from` 和 `from_dataset` |
| `model_config` | — | Pydantic v2 配置项 |
| `model_dump()` | — | Pydantic v2 序列化方法 |
| `model_validate()` | — | Pydantic v2 反序列化方法 |
| `dict[str, Any]` | — | Pyantic schema 通用类型 |

### A.4 CLI / 插件

| 中文 | 英文 | 含义 |
|---|---|---|
| CLI | Command Line Interface | `ossie` 命令行工具（Go） |
| Cobra | — | GitHub 开源 CLI 框架 |
| 插件 | Plugin | `~/.ossie/plugins/<name>/` 下的 converter |
| Plugin Manifest | `plugin.yaml` | 描述 converter 名称、平台、invoke 命令 |
| Plugin Spec | `ossie_plugin_spec` | manifest schema 版本（如 `0.1.0`） |
| Plugin Discovery | — | 扫描 `~/.ossie/plugins/` 找到 `plugin.yaml` |
| IPC | Inter-Process Communication | 进程间通信（JSON over stdin/stdout） |
| Plugin Invoker | `ossie-<vendor>` | 实际 converter 可执行文件 |
| `OSSIE_PLUGIN_DIR` | — | 环境变量，覆盖默认 `~/.ossie/plugins/` |
| Stub | — | 打印 "not yet implemented" 的未实现命令 |
| `ossie plugin list` | — | 唯一已实现的 CLI 命令 |

### A.5 治理 / Apache

| 中文 | 英文 | 含义 |
|---|---|---|
| Apache Way | — | Apache 6 项价值观（meritocracy / peer-based / consensus 等） |
| PPMC | Podling PMC | 孵化阶段项目管理委员会 |
| Mentor | — | ASF 资深成员 |
| IPMC | Incubator PMC | Apache 孵化器 PMC |
| ICLA | Individual CLA | 个人贡献者许可协议 |
| CCLA | Corporate CLA | 企业 CLA |
| Lazy Consensus | — | 默认通过；除非有 veto |
| Binding +1 | — | 投票有约束力（仅 PPMC 成员） |
| `dev@` | — | 主决策邮件列表 |
| `private@` | — | PPMC 私议列表 |
| Incubating | — | 孵化阶段 podling |
| Squash Merge | — | `.asf.yaml` 强制 main 上只 squash merge |

### A.6 生态 / 横向

| 中文 | 英文 | 含义 |
|---|---|---|
| dbt SL | dbt Semantic Layer | dbt Labs 的语义层（Ossie 互补） |
| MetricFlow | — | dbt Labs 开源 SQL 生成器 |
| Cube.dev | — | 自托管 SaaS 语义层 |
| LookML | — | Looker 私有 DSL |
| MCP | Model Context Protocol | LLM 工具调用协议 |
| Hub-and-Spoke | 中心辐射 | Ossie 架构模式 |
| Point-to-Point | 点对点 | N 工具互连 = N×(N-1) 接缝 |
| Polar is | Apache Polaris | Iceberg REST catalog |
| Iceberg | — | 列式表格式 + REST catalog |
| SQLGlot | — | Python SQL 解析器（validator 用） |
| OSIVendor | — | SDK 的 8 个 vendor 枚举 |
| OSIDialect | — | SDK 的 7 个方言枚举 |
| OSIDataType | — | SDK 的 10 个数据类型枚举 |
| OSIAIContext | `Union[str, AIContextObject]` | SDK 的 AI 上下文联合类型 |
| CustomExtension | `data: str` | SDK 中 `data` 是 JSON 字符串 |
| Dialect Fallback | — | 目标 vendor 缺 → ANSI_SQL → warn |
| `custom_extensions[COMMON]` | — | 跨厂商通用的扩展命名空间 |
| `rely.at_most_one_match` | — | Databricks 特性 → 转 Ossie `unique_keys` |
| One-to-Many | 一对多 | Wisdom `ONE_TO_MANY` → 反向 `from/to` |
| Lossless / Lossy | — | 转换后是否保留所有信息 |
| `STASH_VERSION` | — | converter 用 `_v: 1` 标记扩展格式版本 |

## B. 字段速查表

### B.1 顶层字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `version` | string | ✅ | 常量 `"0.2.0.dev0"` |
| `semantic_model` | array | ✅ | 顶层 SemanticModel 列表 |
| `dialects` | array | — | （SDK）声明的方言 |
| `vendors` | array | — | （SDK）声明的 vendor |

### B.2 SemanticModel 字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `name` | string | ✅ | 文件内唯一 |
| `description` | string | — | 人类可读 |
| `ai_context` | string \| object | — | AI 注解 |
| `datasets` | array | ✅ | 至少 1 个 |
| `relationships` | array | — | |
| `metrics` | array | — | |
| `custom_extensions` | array | — | |

### B.3 Dataset 字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `name` | string | ✅ | |
| `source` | string | ✅ | `db.schema.table` 或子查询 |
| `primary_key` | array | — | 单列或复合 |
| `unique_keys` | array of arrays | — | 多组唯一约束 |
| `description` | string | — | |
| `ai_context` | string \| object | — | |
| `fields` | array | — | |
| `custom_extensions` | array | — | |

### B.4 Field 字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `name` | string | ✅ | |
| `expression` | Expression | ✅ | 标量 SQL |
| `dimension` | object | — | `is_time` 标记 |
| `label` | string | — | 分类标签 |
| `description` | string | — | |
| `datatype` | enum | — | 见下表 |
| `ai_context` | string \| object | — | |
| `custom_extensions` | array | — | |

### B.5 Metric 字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `name` | string | ✅ | |
| `expression` | Expression | ✅ | **允许聚合**的 SQL |
| `description` | string | — | |
| `datatype` | enum | — | 输出结果类型 |
| `ai_context` | string \| object | — | |
| `custom_extensions` | array | — | |

### B.6 Relationship 字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `name` | string | ✅ | |
| `from` | string | ✅ | 多端 |
| `to` | string | ✅ | 一端 |
| `from_columns` | array | ✅ | |
| `to_columns` | array | ✅ | |
| `ai_context` | string \| object | — | |
| `custom_extensions` | array | — | |

### B.7 DataType 枚举（10 种）

| 值 | 含义 |
|---|---|
| `String` | 变长 Unicode |
| `Integer` | 精确整数 |
| `Decimal` | 精确 base-10 |
| `Float` | 近似浮点 |
| `Boolean` | 布尔 |
| `Date` | 日，无时区 |
| `Time` | 时，无日期 |
| `DateTime` | 当地日期+时间 |
| `DateTimeTz` | 含时区信息 |
| `Opaque` | 已知但不在可移植词汇表 |

### B.8 Dialect 枚举（7 种）

| 值 | sqlglot 支持 |
|---|---|
| `ANSI_SQL` | ✅ |
| `SNOWFLAKE` | ✅ |
| `DATABRICKS` | ✅ |
| `BIGQUERY` | ✅ |
| `MDX` | ❌ 跳过 |
| `TABLEAU` | ❌ 跳过 |
| `MAQL` | ❌ 跳过 |

### B.9 CustomExtension 字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `vendor_name` | string | ✅ | 自由字符串 |
| `data` | string | ✅ | **JSON 字符串** |

### B.10 AIContext 字段

> 详见 [§2.8 AI 上下文](02-核心规范.md) 主章节。

## C. Converter 速查

| Vendor | 安装 | CLI 命令 |
|---|---|---|
| Snowflake | `uv tool install ossie-snowflake` | `ossie-snowflake -i in.yaml -o out.yaml` |
| dbt | `uv tool install ossie-dbt` | `ossie-dbt msi-to-osi` / `osi-to-msi` |
| Databricks | `uv tool install ossie-databricks` | `ossie-databricks export/import` |
| GoodData | `uv add ossie-gooddata` | （库，无 CLI） |
| GSF | `uv tool install ossie-gsf` | `ossie-gsf export/import` |
| Honeydew | `uv tool install honeydew-osi` | `honeydew-osi osi-to-honeydew` |
| Omni | `uv tool install osi-omni` | `osi-omni export/import` |
| OrionBelt | `uv tool install ossie-orionbelt` | `ossie-orionbelt obml-to-osi` / `osi-to-obml` |
| Wisdom | `uv tool install ossie-wisdom` | `ossie-wisdom wisdom-to-osi` / `osi-to-wisdom` |
| Salesforce | Maven shade jar | `java -jar ... toSF/toOSI` |
| Polaris | Maven shade jar | `java -jar ... import/export` |

## D. 错误消息速查

> v1.1 扩展：7 → 30+ 错误。组织按 [Schema]（JSON Schema）/ [Unique]（唯一性）/ [Reference]（引用）/ [SQL]（sqlglot）/ [Python SDK]（apache-ossie）/ [Converter]（每家 vendor）。

### D.1 JSON Schema 错误

| 错误 | 触发条件 | 修复 |
|---|---|---|
| `'semantic_model' is a required property` | 缺顶层字段 | 添加 `semantic_model: [...]` |
| `'foo' is not of type 'array'` | 字段是 object 而非 array | 改为列表 |
| `'foo' is not of type 'string'` | 字段是 number/bool 而非 string | 加引号 |
| `Additional properties are not allowed` | 字段不在 schema 白名单 | 删除或移入 `custom_extensions` |
| `minItems: 1` 违反 | array 为空 | 加至少 1 个元素 |
| `'foo' is not one of ['String', 'Integer', ...]` | datatype 拼写错 | 查 §B.7 Enum |

### D.2 唯一性错误

| 错误 | 触发条件 | 修复 |
|---|---|---|
| `Duplicate field name 'X' in dataset 'Y'` | 同 dataset 内重名字段 | 重命名 |
| `Duplicate metric name 'X'` | 同 model 内重名 | 重命名 |
| `Duplicate relationship name 'X'` | 同 model 内重名 | 重命名 |
| `Duplicate dataset name 'X'` | 同 model 内重名 | 重命名 |

### D.3 引用完整性错误

| 错误 | 触发条件 | 修复 |
|---|---|---|
| `Relationship 'X' references unknown dataset 'Y'` | `from`/`to` 指向不存在 | 检查 dataset 名 |
| `Metric 'X' references unknown field 'Y'` | 度量表达式引用字段不存在 | 检查字段名 |
| `Cross-model reference not allowed` | Metric 跨 model 引用字段 | 合并 model 或用 ontology |

### D.4 SQL 错误

| 错误 | 触发条件 | 修复 |
|---|---|---|
| `Invalid expression` | sqlglot 解析失败 | 改 SQL 或换方言 |
| `sqlglot not installed` | 缺 sqlglot 依赖 | `pip install sqlglot` |
| `Bare column reference in metric` | Metric 表达式没聚合 | 加 SUM/AVG/COUNT |
| `Aggregation in field` | Field 表达式用了聚合 | 移到 Metric |
| `Unknown dialect 'X'` | dialect 拼写错 | 查 §B.8 Enum |
| `MDX/TABLEAU/MAQL not parseable` | sqlglot 不支持 | 默认跳过 |

### D.5 Python SDK 错误

| 错误 | 触发条件 | 修复 |
|---|---|---|
| `ModuleNotFoundError: No module named 'ossie'` | SDK 没装 | `cd python && uv sync` 或 `PYTHONPATH=python/src` |
| `ValidationError: from is a required property` | SDK signature 误用 | 用 `from_dataset='orders'` |
| `ValidationError: 'X' is not a valid OSIDialect` | enum 大小写 | 用 `SNOWFLAKE` 而非 `Snowflake` |
| `ValidationError: 'Opaque' is not a valid DataType` | datatype 路径错 | `OSIDataType.OPAQUE` |
| `ValidationError: extra fields not permitted` | SDK 字段名拼错 | 检查 `models.py` |
| `yaml.YAMLError: did not find expected` | YAML 缩进错 | 重新格式化 |

### D.6 各 Converter 错误

| Converter | 错误 | 修复 |
|---|---|---|
| Snowflake | `KeyError: 'SNOWFLAKE'` | 加 `dialect: SNOWFLAKE` 表达式 |
| Snowflake | `Opaque type not supported` | 用 vendor-specific type via `custom_extensions` |
| dbt | `CUMULATIVE_SEMANTICS_LOSS` | Roadmap #39 待 0.2.0+ |
| dbt | `PRIVATE_METRIC_DROPPED` | 临时手动改 Ossie YAML |
| Databricks | `KeyError: 'on'` | YAML 1.2 已用，但裸 `on:` 应避免 |
| Databricks | `source.* wildcard not supported` | 显式列出字段 |
| GoodData | `LDM date instance missing` | 标记 `custom_extensions[GOODDATA].date_dimension: true` |
| Salesforce | `DateTime -> DateTimeTz lossy` | 已知转换；下游导入用 `DateTimeTz` |
| Polaris | `401 Unauthorized` | 加 `--client-id --client-secret` |
| Polaris | `Namespace not found` | 先 `polaris namespaces create` |
| Wisdom | `CARDINALITY_LOSS` | MANY_TO_MANY 待 0.2.0+ |
| Wisdom | `UNSUPPORTED_DIALECT` | 加 ANSI_SQL fallback |
| Honeydew | `Metric not assigned to entity` | 手动指定 entity |

## E. CLI 命令速查

### E.1 Go CLI 现状（部分 stub）

```bash
ossie --version
ossie plugin list                    # ✅ 已实现
ossie plugin install <name>          # 🚧 STUB
ossie plugin remove <name>           # 🚧 STUB
ossie convert --to <vendor> -i <in>  # 🚧 STUB（今天请用 vendor 自带 CLI）
ossie validate <file.yaml>           # 🚧 STUB（今天请用 uv run validation/validate.py）
```

### E.2 Python validator

```bash
# 推荐方式（PEP 723 inline 自动管理依赖）
uv run validation/validate.py examples/tpcds_semantic_model.yaml

# 指定 schema
uv run validation/validate.py my.yaml --schema /tmp/experimental.json
```

## F. FAQ

### F.1 Ossie 与 dbt Semantic Layer、Cube、LookML 是什么关系？

**Hub-and-Spoke vs 独立产品**。Ossie 是**互操作规范**——dbt/Cube/LookML 都可以通过 converter 接入 Ossie，反向亦然。这不替代它们，而是让它们能互相转换。

### F.2 为什么我应该用 Ossie 而不是直接写 dbt semantic_models？

如果你只在一个工具栈里用，写 dbt semantic_models 即可。Ossie 的价值在于**跨工具一致性**——同一份模型在 Snowflake / Databricks / dbt / Tableau 都可用。

### F.3 OSSIE 还在变，0.2.0.dev0 能用吗？

> ⚠️ **实现状态提醒**：0.2.0.dev0 是 DRAFT 版本，schema 在 0.2.0 正式发布前可能继续变更。生产环境建议锁版本号到 `0.1.1`（已发布稳定版）。

### F.4 CLI 的 `ossie convert` 怎么用？

今天不能用。请直接用 vendor 自带的 CLI：

```bash
# 等价命令
ossie-snowflake -i model.yaml -o snowflake_model.yaml
ossie-dbt osi-to-msi -i model.yaml -o semantic_manifest.json
```

### F.5 compliance/ 目录是空的，正常吗？

正常。`compliance/` 是规划中的"跨 converter 合规测试套件"。当前各 converter 的测试在各自 `tests/` 目录。

### F.6 本体层（ontology）必须用吗？

不必。TPC-DS 示例就是纯结构模型，没有 ontology。本体层是给"跨模型业务概念对齐"这种高级场景准备的。

### F.7 怎么给 Ossie 提反馈？

1. 小问题：去 GitHub Issues
2. 想法/讨论：去 GitHub Discussions
3. 实质贡献：在 `dev-subscribe@ossie.apache.org` 订阅邮件列表，发起讨论

## G. 参考链接

| 类别 | 链接 |
|---|---|
| 仓库 | https://github.com/apache/ossie |
| 官网 | https://ossie.apache.org/ |
| 项目文档 | https://ossie.apache.org/docs/ |
| Spec 源 | `core-spec/spec.md` + `osi-schema.json` |
| Apache Way | https://www.apache.org/foundation/how-it-works.html |
| ICLA | https://www.apache.org/licenses/icla |
| ASF Generative Tooling | https://www.apache.org/legal/generative-tooling.html |
| JSON Schema Draft 2020-12 | https://json-schema.org/specification-links.html#draft-7 |
| SQLGlot | https://github.com/tobymao/sqlglot |

## H. 本手册来源文件索引

| 章节 | 主要源文件 |
|---|---|
| 序章 | `README.md`、`docs/index.md`、`converters/README.md` |
| 第 1 章 | `README.md`、`CONTRIBUTING.md`、`.asf.yaml`、`.github/workflows/` |
| 第 2 章 | `core-spec/spec.md`、`core-spec/osi-schema.json`、`core-spec/spec.yaml` |
| 第 3 章 | `core-spec/expression_language.md`、`validation/validate.py` |
| 第 4 章 | `examples/tpcds_semantic_model.yaml`、`examples/flights.yaml` |
| 第 5 章 | `validation/validate.py`、`.github/workflows/*.yml` |
| 第 6 章 | `converters/README.md`、`cli/cmd/plugin/` |
| 第 7 章 | `converters/<vendor>/README.md`、`converters/<vendor>/pyproject.toml` 或 `pom.xml` |
| 第 8 章 | `python/src/ossie/models.py`、`python/pyproject.toml` |
| 第 9 章 | `cli/cmd/*.go`、`cli/internal/plugin/`、`cli/.goreleaser.yaml` |
| 第 10 章 | `ontology/ontology.md`、`ontology/ontology.json`、`examples/flights.yaml` |
| 第 11 章 | `CONTRIBUTING.md`、`ROADMAP.md`、`docs/index.md`、`docs/working_groups.md` |

## I. 概念索引（反向查询）

> v1.2 新增。每条术语 → 出现该术语的章节 + anchor。
> 用法：在某术语处查阅"它在哪几章被定义/讨论"。

### I.1 核心构造

| 术语 | 主章节 |
|---|---|
| `SemanticModel` | §2.1 顶层、§2.2 字段、§4.1 最小示例、§10.3 ontology 映射 |
| `Dataset` | §2.3 字段、§4.2 复合主键、§7 converter 映射 |
| `Field` | §2.4 字段、§4.3 多 dialect 写法、§3.2 dialect 选择 |
| `Relationship` | §2.5、§4.4 cross-dataset、§6.4 join 翻译 |
| `Metric` | §2.6、§4.5 cross-dataset metric、§7 各 converter 表达 |
| `OSIDocument` | §2.1、§8.1 SDK 入口 |

### I.2 关键设计

| 术语 | 主章节 |
|---|---|
| `datatype` vs `is_time` 分离 | §2.0.1、§2.4 字段、§4.3 时间维度示例 |
| 复合主键（composite primary keys） | §2.0.2、§2.3、§4.2 TPC-DS 实例 |
| `custom_extensions` 往返保真 | §2.0.3、§2.7、§6.3 转换器契约 |
| `ai_context` 双形态 | §2.0.4、§2.8、§10.8 业务语义 |
| `from_dataset` 别名 | §2.0.5、§8.2 模块全景 |
| 多 dialect 表达式 | §3.2、§4.3、§6.2 转换器契约 |
| 7 种 SQL 方言 | §3.2、§B.8 |
| 10 种 DataType | §2.4、§B.7 |
| 8 种 ValueType | §10.4、§10.4.1 |
| `Ossie_SQL_2026` 提案方言 | §3.6 |
| Hub-and-Spoke 经济学 | §0.2 序章、§6.1、§1.1 |
| 本体层（Ontology） | §10 全章、§2.0 取舍 |
| 实体类型 / 值类型 | §10.2、§10.4 |
| `derived_by` 派生 | §10.5.5、§A.2 |
| `OntologyMap` | §10.3、§10.6、§A.2 |

### I.3 工具与生态

| 术语 | 主章节 |
|---|---|
| 11 个 vendor converters | §0.2 序章、§7 全章 |
| 9 Python + 2 Java 转换器 | §1.1、§7 全章 |
| `validation/validate.py` | §5 全章、§3.5 SQL 校验 |
| `apache-ossie` Python SDK | §8 全章 |
| Pydantic v2 模型 | §8.1、§8.4、§2.2 |
| Go CLI Cobra | §9.1、§9.8 |
| 4 处 CLI stub | §9.3、§A.4、§FAQ F.4 |
| Plugin Manifest | §6.5、§9.4、§A.4 |
| IPC JSON over stdin/stdout | §9.5、§A.4 |
| Apache 治理 | §11 全章、§11.1 |
| `dev@ossie.apache.org` | §11.4 沟通渠道、§G.2 |
| ≥3 binding +1 投票 | §11.3 投票流程 |
| 合规测试套件（compliance/） | §11.7、§FAQ F.5 |

### I.4 集成模式

| 模式 | 主章节 |
|---|---|
| 模式 A：单一真相源 | reference-architectures.md §模式 A |
| 模式 B：AI Agent 推理 | reference-architectures.md §模式 B |
| 模式 C：Catalog 集成（Polaris） | reference-architectures.md §模式 C、§7.3.1 |
| 模式 D：多团队 Hub | reference-architectures.md §模式 D |

> 详见 [reference-architectures.md](reference-architectures.md)。

### I.5 错误与速查

| 类别 | 主章节 |
|---|---|
| JSON Schema 错误 | §D.1、§5.4 |
| 唯一性错误 | §D.2 |
| 引用完整性错误 | §D.3 |
| SQL 解析错误 | §D.4、§3.5、§5.3 |
| SDK 反序列化错误 | §D.5、§8.4 |
| Converter 特定错误 | §D.6、§7 全章 |
| 30+ 症状排查 | [troubleshooting.md](troubleshooting.md) 全章 |

## J. 手册维护指南

> v1.2 新增。面向贡献者：如何修改、发布、校对。

### J.1 贡献流程

1. **fork + branch**：从 `apache/ossie` main 创建 `handbook-<topic>` 分支
2. **本地编辑**：在 `handbook/src/*.md` 直接修改；按 v1.1 模式加英文 Abstract + 三类读者 callout + 章末 cheat-sheet
3. **本地构建**：`mkdocs build --strict` 必须 0 警告
4. **PR**：标题 `handbook: <一句话描述>`；body 列出修改章节 + 关键校验结果
5. **review**：≥1 PMC member +1（Apache Way）；spec 字段变更需 ≥3 binding +1（见 §11.3）
6. **merge**：squash merge only（`.asf.yaml` 限定）；commit message 用 `handbook: vX.Y <一句话>`

参考 `CONTRIBUTING.md:1-287`（仓库根目录）。

### J.2 校对 checklist（提交前自检）

- [ ] **path:line 引用**：每个 `file:line` 引用必须实际存在于当前仓库。可用：
  ```bash
  sed -n 'L,p' core-spec/osi-schema.json  # 验证 schema 引用
  sed -n 'L,p' python/src/ossie/models.py  # 验证 SDK 引用
  ```
- [ ] **mkdocs build --strict**：0 警告（包含 broken link、missing file、unused variable）
- [ ] **英文 Abstract**：每章 ≤200 字；摘要与正文一致
- [ ] **三类读者侧栏**：每章顶部必有【为用户】【为开发者】【为架构师】
- [ ] **章末 cheat-sheet**：每章末尾必须有 "X.X 📌 章节要点速查" 表
- [ ] **诚实标注**：Go CLI stub / compliance 占位 / ontology block 等"已知未做"项在 README、00-序章、对应章节均提及
- [ ] **PDF 同步**：若新增/删除章节，运行 `python3 build_pdf.py` 重生成 PDF

### J.3 版本节奏

| 版本 | 状态 | 触发条件 |
|---|---|---|
| v1.0 | 2026-08-10 | 发布标准（22 错误修复 + ASF headers） |
| v1.1 | 2026-08-11 | 优质文档标准（6 新章 + 19 abstract + 56 glossary + 30+ 故障） |
| v1.2 | 2026-08-11 | 优质文档标准补完（附录 I/J + Prev/Next + §2/§10 深化 + 修 v1.1 README 不一致） |
| v1.3+ | 待规划 | 5 章全部深化、SCOPE 文档、changelog、英文版手册 |

### J.4 常用命令

```bash
# 1. 安装依赖（首次）
cd handbook && uv venv && source .venv/bin/activate
uv pip install mkdocs mkdocs-material pymdown-extensions reportlab

# 2. 实时预览（带 hot reload）
mkdocs serve
# 浏览器打开 http://127.0.0.1:8000

# 3. 严格构建（CI 用）
mkdocs build --strict

# 4. 重生成 PDF（v1.2 起 ReportLab + STHeiti.ttc）
python3 build_pdf.py

# 5. 验证手册命令（关键路径）
python3 ../validation/validate.py ../examples/tpcds_semantic_model.yaml

# 6. 添加 ASF license header（仅新文件）
python3 scripts/add_license_headers.py

# 7. 发版
git add -A && git commit -m "handbook: vX.Y <description>"
git tag handbook-vX.Y
```

### J.5 文件命名约定

- 章节用两位数字前缀：`00-序章.md`、`01-项目全景.md` ... `12-附录.md`
- v1.1+ 章节用英文短横线：`quickstart.md`、`troubleshooting.md` 等（已在 mkdocs.yml 注册）
- `index.md` 是首页；`verification.md` 是验证日志；二者不放进章节编号

---

**手册维护**：基于 `88e0011` 提交快照 + v1.2 增量。
**许可**：本手册内容遵循 Apache License 2.0；verbatim 引用均标注 `path:line` 溯源。